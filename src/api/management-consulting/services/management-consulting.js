'use strict';

/**
 * management-consulting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::management-consulting.management-consulting');
