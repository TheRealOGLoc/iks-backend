'use strict';

/**
 * management-consulting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::management-consulting.management-consulting');
